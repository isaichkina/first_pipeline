general:
steps:
  mtaBuild:
    buildTarget: 'CF'
    
  cloudFoundryDeploy:
    deployTool: 'mtaDeployPlugin'
    deployType: 'standard'
    cloudFoundry:
      org: '932f74detrial'
      space: 'dev'
      credentialsId: 'CF_CREDENTIALSID'
